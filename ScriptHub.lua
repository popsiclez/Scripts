  local StarterGui = game:GetService("StarterGui")
	
		StarterGui:SetCore("SendNotification", {
			Title = "Popsiclez - Script Hub";
			Text = "Hey, for now we are disabling the script hub, so just execute the scripts manually. the link with all of them has been copied."
		})

setclipboard("https://github.com/popsiclez/Scripts")
